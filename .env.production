# Production Environment Configuration
# These values will be used by default in production builds
# Override these in your deployment platform (Vercel, Netlify, etc.) as needed

# LangGraph API Configuration
NEXT_PUBLIC_API_URL=https://nuflow.vercel.app
